# GitHub Container Registry Configuration
GHCR_OWNER=your-github-username
GHCR_TOKEN=your-github-personal-access-token
TAG=latest

# PostgreSQL Database Configuration
POSTGRES_USER=Heimr
POSTGRES_PASSWORD=change-this-secure-password
POSTGRES_DB=heimr_db

# Backend Configuration
PORT=5000
NODE_ENV=production
BACKEND_ORIGIN=https://yourdomain.com
FRONTEND_ORIGIN=https://yourdomain.com

# Security Keys (Generate secure random strings)
SESSION_SECRET=generate-a-secure-random-string-min-32-chars
ENCRYPTION_KEY=generate-a-64-character-hex-string-for-encryption
ADMIN_KEY=generate-a-secure-admin-key

# Database URL (auto-generated, no need to change if using docker-compose)
# DATABASE_URL will be set in docker-compose.yml
